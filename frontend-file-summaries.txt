{
  "frontend/src/App.tsx": "Root application component with cart state management where `CartItem.id` is now a string (the product's string ID) for reliable deduplication; `addToCart` deduplicates by `productId`, and `removeFromCart`/`updateQuantity` accept string IDs.",
  "frontend/src/components/Header.tsx": "Sticky teal header with Gadget Galaxy branding, view-aware navigation including a conditional Admin Panel link visible only to authenticated admin users, Internet Identity login/logout, and cart button with item count badge.",
  "frontend/src/components/Hero.tsx": "Full-width hero section with teal gradient background, headline, subtitle, and CTA buttons linking to the products section.",
  "frontend/src/components/ProductCatalog.tsx": "Responsive product grid fetching products from the backend with search, category filter, and sort controls; `handleAddToCart` now passes `product.id` (string) as the cart item `id` for consistent deduplication, and the `getProductImage` helper covers additional product types (lamp, stand, smart home).",
  "frontend/src/components/CartPanel.tsx": "Slide-in cart drawer updated to accept string `id` parameters in `onRemove` and `onUpdateQuantity` callbacks, matching the updated `CartItem.id: string` type in App.tsx.",
  "frontend/src/components/Footer.tsx": "Dark footer with Gadget Galaxy branding, quick links, contact info, dynamic copyright year, and caffeine.ai attribution link.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations including user profile, admin role check, product CRUD, order management, and Stripe payment integration (useIsStripeConfigured, useSetStripeConfiguration, useCreateCheckoutSession, useGetStripeSessionStatus) with proper actor dependency handling and cache invalidation.",
  "frontend/src/index.css": "Global styles with OKLCH teal color system, Inter font import, and Tailwind base/component/utility layers.",
  "frontend/tailwind.config.js": "Tailwind configuration with Inter font, teal/dark/page color tokens, hero gradient background image, and standard shadcn/ui theme extensions.",
  "frontend/src/components/CheckoutFlow.tsx": "Checkout page showing order summary with itemized list and totals; updated to use string-keyed cart items and adds an `onError` fallback to product images in the order summary list.",
  "frontend/src/components/OrderSuccess.tsx": "Order confirmation screen displayed after a successful order placement, showing the order ID, success message, and buttons to view order history or continue shopping.",
  "frontend/src/components/OrderHistory.tsx": "Authenticated order history page that fetches orders via useGetOrdersForCaller and product details via useGetAllProducts (replacing the removed static PRODUCTS array), building a productId-to-Product lookup map to display each order's items with image, name, quantity, and price; includes loading skeletons, error state, and empty state.",
  "frontend/src/components/UserProfileSetup.tsx": "Modal dialog that appears for first-time authenticated users to enter their display name, using useGetCallerUserProfile to detect missing profiles and useSaveCallerUserProfile to persist the name; prevents flash with proper actor-dependency state handling.",
  "frontend/src/components/PaymentSuccess.tsx": "Payment success screen that verifies the Stripe session via getStripeSessionStatus, submits the order using cart items from props or sessionStorage fallback, displays the confirmed order ID, and provides navigation to order history or catalog.",
  "frontend/src/components/PaymentFailure.tsx": "Payment failure/cancellation screen with failure icon, reassurance message, and buttons to retry checkout or continue shopping; uses `onBackToShopping` prop name to match App.tsx.",
  "frontend/src/components/PaymentSetup.tsx": "Admin-only floating button and dialog for configuring Stripe payments; collects the Stripe secret key and allowed countries, validates input, saves via useSetStripeConfiguration, and shows a reconfigure button when already set up.",
  "frontend/src/components/AdminPanel.tsx": "Admin-only product management panel with add/edit/delete functionality, a Stripe configuration status banner, delete confirmation dialogs, and access guard; integrates useIsStripeConfigured to show setup prompt when payments are not yet configured."
}